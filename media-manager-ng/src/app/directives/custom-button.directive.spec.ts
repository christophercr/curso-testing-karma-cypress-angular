import { CustomButtonDirective } from './custom-button.directive';

xdescribe('CustomButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomButtonDirective();
    expect(directive).toBeTruthy();
  });
});
