import { IfViewportDirective } from './if-viewport.directive';

xdescribe('IfViewportDirective', () => {
  it('should create an instance', () => {
    const directive = new IfViewportDirective();
    expect(directive).toBeTruthy();
  });
});
